/*6) Desenvolver um programa que apresente os quadrados dos números inteiros de 2 a 50. */
let cont = 2;
let multi 
do{

    multi = cont **2

    console.log(`${multi}`)
    cont ++
}while(cont <= 50)