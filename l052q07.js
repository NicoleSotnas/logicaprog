/* Desenvolver um programa que apresente as potências de 2, variando de 0 a 10. */
let cont = 1;
let num = 2
let multi 
do{

    multi = num ** cont

    console.log(`${multi}`)
    cont ++
}while(cont <= 10)